version: "3"

services:
  autoconf:
    image: appgw.azurecr.io/modsecurity:latest
    build:
      dockerfile: docker/Dockerfile
      context: .
    volumes:
      - ./:/microsoft/modsecurity
    working_dir: /microsoft/modsecurity
    entrypoint: ./autogen.sh

  configure:
    image: appgw.azurecr.io/modsecurity:latest
    build:
      dockerfile: docker/Dockerfile
      context: .
    volumes:
      - ./:/microsoft/modsecurity
    working_dir: /microsoft/modsecurity
    entrypoint: 
      - ./configure
      - CXXFLAGS=-std=c++11
      - CFLAGS=-fPIC
      - --enable-standalone-module
      - --disable-mlogc

  make:
    image: appgw.azurecr.io/modsecurity:latest 
    build:
      dockerfile: docker/Dockerfile
      context: .
    volumes:
      - ./:/microsoft/modsecurity
    working_dir: /microsoft/modsecurity
    entrypoint: make
